{
  "name": "@contentpulse/payload",
  "version": "0.1.0",
  "description": "ContentPulse adapter for Payload CMS v3 — semantic decay & freshness engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["contentpulse", "contentful", "content-freshness", "semantic-decay"],
  "repository": { "type": "git", "url": "https://github.com/mturac/content-pulse" },
  "dependencies": {
    "@contentpulse/core": "workspace:*"
  },
  "peerDependencies": {
    "payload": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "license": "MIT"
}
